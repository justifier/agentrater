package com.agent.rater.dao;

import com.agent.rater.model.Customer;

public interface CustomerDao extends GenericCustomerDao<Customer> {

    //TODO right now we don't need this but some day we might!
}
